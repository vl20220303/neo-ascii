# About neo_ascii
I initially started this project to overlay the code rain effect onto Agent Smith's face for a cool desktop background.<br><br>Now, `neo_ascii` is a modular library that supports many image effects and converts images, gifs, and videos into ASCII-style media.

# Typical Usage
Typically, there are three main functional blocks involved with producing an image, GIF, or video with `neo_ascii`: <br>
## The preprocessing
Any animated effect masks will need to be prepared at the very beginning. For example, 
## The pipeline function
## The exporter
